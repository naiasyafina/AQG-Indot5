# AQG — Automatic Question Generator
# Requirements untuk dijalankan lokal:
#   pip install -r requirements.txt
#   streamlit run app.py

# ── Core UI ────────────────────────────────────────────────────
streamlit>=1.45.0

# ── Deep Learning & Model ──────────────────────────────────────
torch>=2.6.0
transformers>=4.50.0
sentencepiece>=0.2.0
safetensors>=0.5.0
huggingface-hub>=0.29.0

# ── Semantic Chunking (opsional — fallback otomatis jika tidak ada) ──
sentence-transformers>=3.4.0

# ── NLP Utilities ─────────────────────────────────────────────
nltk>=3.9

# ── Document Parsing ───────────────────────────────────────────
pdfplumber>=0.11.0
python-docx>=1.1.0
python-pptx>=1.0.2

# ── Qwen via Groq (opsional — fallback heuristic jika tidak ada) ─
groq>=0.9.0

# ── HTTP & Environment ─────────────────────────────────────────
requests>=2.32.0
python-dotenv>=1.0.1

# ── Data Handling ──────────────────────────────────────────────
pandas>=2.0.0
